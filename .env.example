# === LLM 質化分析（選用）===
# 設定任一個即可啟用 AI 撰寫的「執行摘要 / 護城河 / 風險 / 結論」。
# 兩者皆未設定時，自動降級為純數據模板（程式不會崩潰）。
# 優先順序：Anthropic > OpenAI。

# Anthropic (Claude)
ANTHROPIC_API_KEY=
# 可選：覆寫模型（預設 claude-opus-4-8）
# ANTHROPIC_MODEL=claude-opus-4-8

# OpenAI (GPT)
OPENAI_API_KEY=
# 可選：覆寫模型（預設 gpt-4o）
# OPENAI_MODEL=gpt-4o

# 可選：質化區塊每段最大 token 數（預設 1500）
# LLM_MAX_TOKENS=1500

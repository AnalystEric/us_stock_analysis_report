# Streamlit secrets 範例 —— 請「勿」提交填了真實金鑰的版本到 GitHub。
# 在 Streamlit Community Cloud：App → Settings → Secrets 貼上；平台會同時以環境變數注入，
# 因此 config.py 讀取 os.environ 即可取得。本地測試才把此檔另存為 secrets.toml。
#
# 皆留空 = 只跑純數據模板（不燒 LLM 額度）。

ANTHROPIC_API_KEY = ""
OPENAI_API_KEY = ""
